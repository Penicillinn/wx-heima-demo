/* components/imgIcon/imgIcon.wxss */
.img-wrp {
  width: 100rpx;
  height: 100rpx;
  position: relative;
  /* background-color: aqua; */
  margin: 10rpx 8rpx;
}
image {
  width: 100%;
  height: 100%;
  border-radius: 10rpx;
}
icon {
  position: absolute;
  top: -20rpx;
  right: -20rpx;
  color: var(--themeColor);
}
